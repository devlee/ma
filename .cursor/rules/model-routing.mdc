---
description: 模型分工规范：设计讨论用 Fable 5.1，写代码用 Cursor 内置模型（如 Grok）
alwaysApply: true
---

# 模型路由规范

本仓库按任务类型选择模型。规则会注入上下文作为偏好与提醒；**真正切换模型需用户在聊天模型选择器中切换，或通过下方自定义 Subagent 委派**。

## 设计讨论 → Claude Fable 5.1

适用于（优先使用 **Claude Fable 5.1**）：

- 产品/业务方案讨论、策略推演、需求分析
- UX / 信息架构 / 交互流程讨论
- 技术方案选型、架构权衡、RFC、实现前规划
- 评审方案、对比方案、澄清目标与约束

**操作建议：**

1. 开启新对话时，将模型选为 **Claude Fable 5.1**（或带 Thinking 的 Fable 5.1 变体）
2. 或使用自定义 Subagent：`/design-discuss`（已固定为 Fable 5.1）
3. 本阶段以讨论与结论为主，默认不写代码、不创建/修改文件，除非用户明确要求落地

## 写代码 / 改仓库 → Cursor 内置模型（推荐 Grok）

适用于（优先使用 **Cursor 内置模型**，推荐 **Grok 4.6**；也可使用 Composer 等 Cursor Models 池内模型）：

- 编写、修改、重构代码
- 创建/删除/重命名文件与目录
- 补测试、修 bug、跑命令验证
- 落地已确认的设计方案

**操作建议：**

1. 从设计转入实现时，将模型切换为 **Grok 4.6**（或 Composer 等 Cursor 内置模型）
2. 或使用自定义 Subagent：`/code-implement`（已固定为 Grok 4.6）
3. 实现阶段遵循仓库既有约定，优先小而可审阅的改动

## Agent 行为要求

- 识别到任务类型变化时，**主动提醒用户切换模型**（例如：设计结束准备写代码 → 提醒切到 Grok）
- 不要声称自己已切换模型；当前对话模型由用户选择或 Subagent 配置决定
- 混合任务时：先用 Fable 完成设计结论，再切到 Grok/Cursor 内置模型落地
- 委派子任务时：设计类委派 `design-discuss`，实现类委派 `code-implement`，并尊重其 `model` 配置，不要覆盖

## 速查

| 任务 | 模型 | 入口 |
| --- | --- | --- |
| 设计 / 方案 / 讨论 | Claude Fable 5.1 | 模型选择器 或 `/design-discuss` |
| 写代码 / 建文件 / 改仓库 | Grok 4.6（Cursor 内置） | 模型选择器 或 `/code-implement` |
